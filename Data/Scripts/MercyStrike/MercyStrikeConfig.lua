-- Example overrides (optional)
return {
    hpThreshold    = 0.10, -- 10%
    applyChance    = 0.18, -- 18%
    includeAnimals = false,
    logging        = { core = true, probe = true, apply = true, skip = true },
}
